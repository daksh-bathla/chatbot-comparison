# Streamlit Secrets Template
# Copy to .streamlit/secrets.toml for local development
# DO NOT commit actual secrets - only use for local dev
# In production (Streamlit Cloud), set via dashboard Settings → Secrets

GROQ_API_KEY = "gsk_your_api_key_here"
